%h2 Artists
- @artists.each_slice(6) do |artists|
	.row
		- artists.each do |artist|
			.col-md-2	
				= link_to artist do
					= image_tag artist.image.url, class: 'artist_img'
				%br/
				%h5= artist.name